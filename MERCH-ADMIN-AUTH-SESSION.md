# 商家端登录会话方案说明

## 1. 文档目的

这份文档给商家端前端工程师看，说明当前商家后台登录态的后端实现已经变成什么样，以及前端是否需要跟着调整。

适用范围：

- 路由前缀：`/merchant-api`
- 认证相关接口：
  - `POST /merchant-api/auth/login`
  - `POST /merchant-api/auth/logout`
  - `GET /merchant-api/auth/profile`
  - `POST /merchant-api/auth/change-password`

---

## 2. 当前已落地的后端方案

当前商家端采用的是：

- `单 token`
- `服务端会话控制`
- `空闲过期 + 绝对过期`
- `自动续期`

不是：

- 纯 JWT
- refresh token 双 token 方案

后端仍然会在登录成功后返回一个随机 `token`，前端继续像现在一样保存并随请求带上即可。

---

## 3. 现在的会话规则

### 3.1 登录成功后

后端会做两件事：

1. 在商家管理员表 `fa_wlktour_merch_admin.token` 中写入当前 token  
2. 在服务端缓存中写入当前 token 的会话元数据

会话元数据包含：

- `issued_at`：签发时间
- `last_active_at`：最后活跃时间
- `absolute_expire_at`：绝对过期时间

### 3.2 空闲过期

- 连续 `2 小时`没有有效请求，会话失效

说明：

- 这里的“有效请求”指带着正确 token 并且通过鉴权的商家端接口请求
- 页面开着不动、用户不操作，不会自动续期

### 3.3 绝对过期

- 从登录成功开始，最长 `7 天`后必须重新登录

说明：

- 即使用户一直在操作，也不会无限续下去
- 超过 7 天后，下一个请求会直接返回未登录

### 3.4 自动续期

- 每次已登录请求成功通过鉴权后，后端都会刷新 `last_active_at`
- 所以只要用户持续使用系统，`2 小时空闲过期`会向后顺延

说明：

- 当前没有单独的“续期接口”
- 续期是后端自动做的，前端不用主动刷新 token

### 3.5 单点登录

- 同一个商家管理员账号再次登录时，旧 token 会立刻失效

表现为：

- A 设备登录后，B 设备再次登录同一账号
- A 设备后续任意接口请求会收到 `401`

### 3.6 主动失效

以下情况当前 token 会立刻失效：

- 用户主动退出登录
- 用户修改密码成功
- 同账号在别处重新登录
- 账号状态变成非 `normal`

### 3.7 缓存清理影响

当前项目默认使用 `File` 缓存。

这意味着：

- 如果服务端清空运行缓存
- 商家端会话缓存也会一起消失
- 用户下一个请求会收到 `401`

这属于当前设计内的正常表现。

---

## 4. 前端最关心的结论

## 4.1 不属于 breaking change 的部分

前端目前**不需要**因为这次改动去改以下内容：

- 登录接口路径不变
- 登录请求参数不变
- 登录成功返回结构不变
- `profile` 返回结构不变
- `logout`、`change-password` 接口路径不变
- 不需要新增 refresh token 存储逻辑
- 不需要额外调用续期接口

也就是说：

- 你们现在如果已经能登录、存 token、请求时带 token
- 基本可以继续工作

## 4.2 前端建议立即补上的部分

前端建议补 3 件事：

1. 全局统一处理 `401`
2. 收到 `401` 后清理本地 token 与用户信息
3. 跳转登录页并提示“登录已过期，请重新登录”

原因很简单：

- 现在后端已经真正有“过期”概念了
- 用户空闲 2 小时、超过 7 天、异地重新登录、后台清缓存，都可能触发 `401`

---

## 5. 前端是否需要改 token 传递方式

当前后端兼容以下几种 token 读取方式：

1. 请求头 `Token: xxx`
2. 请求头 `Authorization: Bearer xxx`
3. 请求参数 `token=xxx`
4. Cookie 中的 `token`

### 当前建议

短期内：

- 如果前端现在一直在用 `Token` 请求头，可以继续用，不会受影响

中期建议：

- 逐步统一成 `Authorization: Bearer <token>`

不推荐继续扩大的方式：

- URL query 传 `token`

原因：

- query 参数更容易进日志、浏览器历史、代理记录

---

## 6. 401 时前端会看到什么

HTTP 状态码：

- `401 Unauthorized`

响应结构仍然是统一结构，例如：

```json
{
  "code": 401,
  "msg": "Session has expired",
  "time": 1770000000,
  "data": null
}
```

也可能是：

```json
{
  "code": 401,
  "msg": "Please login first",
  "time": 1770000000,
  "data": null
}
```

前端不要依赖某一个英文文案做判断，建议以 `HTTP 401` 为准。

推荐策略：

- 只要是商家端接口返回 `401`
- 就视为登录态失效

---

## 7. 前端需要注意的几个场景

### 场景 1：页面挂着不动超过 2 小时

表现：

- 用户回来操作时，第一笔接口请求会收到 `401`

前端建议：

- 弹提示
- 清本地登录态
- 跳登录页

### 场景 2：用户改密码成功

表现：

- 改密接口本次会成功
- 当前 token 会立即失效

前端建议：

- 改密成功后，不要停留在当前登录态
- 直接清 token 并跳登录页

### 场景 3：同账号在别处登录

表现：

- 当前页面下一次请求时收到 `401`

前端建议：

- 提示“账号已在其他地方登录，请重新登录”
- 然后回登录页

### 场景 4：服务端清缓存

表现：

- 用户会突然收到 `401`

前端建议：

- 按正常过期处理，不需要做特殊分支

---

## 8. 前端推荐实现方式

## 8.1 请求拦截器

统一做这件事：

- 从本地存储取 token
- 写入请求头

推荐示例：

```ts
const token = storage.get('merchant_token');

if (token) {
  headers.Authorization = `Bearer ${token}`;
}
```

如果当前前端已经固定使用 `Token` 请求头，也可以先保持：

```ts
const token = storage.get('merchant_token');

if (token) {
  headers.Token = token;
}
```

## 8.2 响应拦截器

统一处理商家端接口 `401`：

```ts
if (response.status === 401) {
  clearMerchantAuth();
  showMessage('登录已过期，请重新登录');
  router.replace('/login');
  return;
}
```

`clearMerchantAuth()` 建议至少清：

- token
- 当前管理员信息
- 当前商家信息
- 依赖登录态的页面缓存

## 8.3 不建议做的事

- 不需要自己实现定时续期任务
- 不需要轮询刷新 token
- 不需要加 refresh token 逻辑

---

## 9. 对现有前端的影响评估

如果你们现在已经有全局 `401` 处理：

- 大概率无需额外改动

如果你们现在没有统一 `401` 处理：

- 建议这次补上

如果你们现在改密成功后仍然停留当前页：

- 建议改成“改密成功后跳登录页”

如果你们现在通过 query 参数传 token：

- 建议尽快改成请求头传递

---

## 10. 联调建议

前后端联调时建议至少验证下面几项：

1. 正常登录后，请求业务接口成功
2. 登录后持续操作，请求 2 小时内不会掉线
3. 人工让 token 失效后，请求返回 `401`
4. 调用退出登录后，再请求接口返回 `401`
5. 修改密码成功后，再请求接口返回 `401`
6. 同账号在第二个浏览器登录后，第一个浏览器再请求返回 `401`

---

## 11. 一句话结论

这次后端改动对前端**不是接口结构变更**，更像是“登录态终于变成真正可过期、可失效、可续期了”。

前端最重要的动作只有一个：

- 把商家端接口的 `401` 统一处理好

